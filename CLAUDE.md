# About EJ

**Name:** EJ

**Roles (separate ventures):**
- Auditing
- Accountancy
- Pickleball operations
- HR
- Financial investments (financial market)

**Clients/audience:** All walks of life (across all ventures)

**Current focus:** Keeping smooth operations across the above.

**How to help:** All-purpose support — writing/reviewing documents, analysis, coding, scheduling, drafting communications.

**Communication style:** Brief and to the point. No fluff, no over-explaining.

# Run lean

- Keep your answers short and skip the filler. Say what matters, then stop.
- Do not re-read files you have already read, or redo work we have already done.
- When I ask you to search across a lot of files, send a cheap helper agent to do it instead of reading everything into our chat.
- Match the model to the job. Use a light, cheap model for simple work like searches, lookups, and small edits, and switch up to the most powerful model only for hard reasoning, planning, and debugging.
- Make small, targeted edits. Do not rewrite a whole file to change a few lines.
- When a command would print a huge wall of text, show me only the part that matters.
- Watch the context bar. When it runs hot, tell me to run /sum and carry on in a fresh session.

# Self-improving system

This directory has three folders. Follow these rules automatically, every time:

- `/raw` — my original, unaltered assets. Only ever add to it. Never reorganize, rename, move, or edit anything already inside.
- `/private` — personal documents (bank statements, IC, contracts). Local-only, gitignored. Never push, commit to a remote, upload, or share this folder or its contents anywhere.
- `/wiki` — an AI-written table of contents that indexes `/raw`. You write and maintain this; I never edit it by hand. When `/raw` changes, update `/wiki` to match.
