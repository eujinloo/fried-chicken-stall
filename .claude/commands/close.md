---
description: End-of-day wrap-up — commit and push all work, update memory, then summarize what changed
---

Run this when EJ is done for the day.

1. **Save the work.** Invoke the `commit` skill to commit everything currently in this directory (excluding `/private`) with a clear message and push it to GitHub. If there's genuinely nothing to commit, note that and skip to step 2.

2. **Update memory.** Invoke the `learn` skill to review this session and save any durable feedback/project/user/reference memories.

3. **Summarize.** Report back in 5-8 lines max:
   - What was committed and pushed (or "nothing to commit" if step 1 was a no-op), with the commit message and branch/remote.
   - What memory notes were saved or updated (from step 2), if any.
   No filler — just the facts EJ needs to know the day's work is saved.
